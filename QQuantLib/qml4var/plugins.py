"""
plugins.py — deprecated.

The myQLM plugin stack (SetParametersPlugin, pdfPluging, MyQPU, etc.) has been
replaced by direct PennyLane QNode evaluation with PyTorch autograd.
This module is kept as an empty placeholder to avoid import errors in any
legacy code that may reference it.
"""
